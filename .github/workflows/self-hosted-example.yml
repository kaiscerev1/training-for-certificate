name: Testing Self-Hosted Runner

on:
    workflow_dispatch:

jobs:
    testing:
        runs-on: [self-hosted-Linux-X64]
        steps:
            - name: Echo Content
              run: |
                echo OK
                sleep 150s

